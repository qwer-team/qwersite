<div class="footer-widget-area">
    {% if entity is not empty %}
        {{ entity.translate( locale ).getContent() | raw }}
    {% endif %}
</div>

<div class="footer-info">
<div class="container_12 clearfix">
<div class="grid_12">
<div class="wrapper"><p>
                                    {{ "copyright" | trans }}
                                    {% autoescape false %}
                                        {{ "develop" | trans }}
                                        {% endautoescape %}
            </p></div></div></div></div>
